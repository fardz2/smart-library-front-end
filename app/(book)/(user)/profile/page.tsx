import { Button } from "@mui/material";
import axios from "axios";
import { useEffect } from "react";

export default async function Page({ params }: { params: { idUser: number } }) {
  return <p>profile</p>;
}
